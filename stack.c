#include "monty.h"

/**
 * stack - stack (LIFO or FILO)
 * @stack: Double pointer
 * @line_number: Line number of the opcode
 * Return: Nothing
 */
void stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;

	glob.fifo = 1;
}
